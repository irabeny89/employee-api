class Employee {
  constructor(employeeId, firstName, lastName, age, joinDate) {
    this.employeeId = employeeId
    this.firstName = firstName
    this.lastName = lastName
    this.age = age
    this.joinDate = joinDate
  }
}

module.exports = Employee